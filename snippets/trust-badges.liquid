{% comment %}
  ============================================
  BADGES DE RASSURANCE - BRESLEV BOOKS
  ============================================

  Badges de confiance pour rassurer les clients
  Inspiré des benchmarks e-commerce

  Usage: {% render 'trust-badges' %}
{% endcomment %}

<section class="trust-badges-section">
  <div class="container">
    <div class="trust-badges-grid">

      {%comment%} Badge 1: Livraison {%endcomment%}
      <div class="trust-badge">
        <div class="trust-icon">
          <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
            <path d="M5 15h20v15H5V15z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M25 20h4l4 4v6h-4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <circle cx="9" cy="32" r="3" stroke="currentColor" stroke-width="2"/>
            <circle cx="29" cy="32" r="3" stroke="currentColor" stroke-width="2"/>
          </svg>
        </div>
        <div class="trust-content">
          <h3 class="trust-title">Livraison Rapide</h3>
          <p class="trust-description">Gratuite dès 59€ - Expédition 24/48h</p>
        </div>
      </div>

      {%comment%} Badge 2: Paiement sécurisé {%endcomment%}
      <div class="trust-badge">
        <div class="trust-icon">
          <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
            <rect x="6" y="12" width="28" height="20" rx="2" stroke="currentColor" stroke-width="2"/>
            <path d="M6 20h28M14 26h6" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            <path d="M20 8v4" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          </svg>
        </div>
        <div class="trust-content">
          <h3 class="trust-title">Paiement Sécurisé</h3>
          <p class="trust-description">SSL - Stripe - PayPal - CB</p>
        </div>
      </div>

      {%comment%} Badge 3: Garantie satisfaction {%endcomment%}
      <div class="trust-badge">
        <div class="trust-icon">
          <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
            <path d="M20 5L8 12v8c0 7.5 5 14 12 17 7-3 12-9.5 12-17v-8L20 5z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M14 20l4 4 8-8" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </div>
        <div class="trust-content">
          <h3 class="trust-title">Satisfait ou Remboursé</h3>
          <p class="trust-description">Retour sous 14 jours</p>
        </div>
      </div>

      {%comment%} Badge 4: Service client {%endcomment%}
      <div class="trust-badge">
        <div class="trust-icon">
          <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
            <circle cx="20" cy="13" r="6" stroke="currentColor" stroke-width="2"/>
            <path d="M10 33c0-5.5 4.5-10 10-10s10 4.5 10 10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            <path d="M26 28h6M26 32h6" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          </svg>
        </div>
        <div class="trust-content">
          <h3 class="trust-title">Service Client</h3>
          <p class="trust-description">Réponse rapide - WhatsApp</p>
        </div>
      </div>

    </div>
  </div>
</section>

<style>
/* ============================================
   TRUST BADGES STYLES
   ============================================ */

.trust-badges-section {
  padding: 3rem 0;
  background-color: var(--color-bg-primary, #ffffff);
  border-top: 1px solid var(--color-border-light, #f0f0f0);
  border-bottom: 1px solid var(--color-border-light, #f0f0f0);
}

.trust-badges-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
}

/* === BADGE === */
.trust-badge {
  display: flex;
  align-items: center;
  gap: 1.25rem;
  padding: 1.5rem;
  background-color: var(--color-bg-tertiary, #f5f5f5);
  border-radius: var(--radius-lg, 0.75rem);
  transition: all 0.3s ease;
}

.trust-badge:hover {
  background-color: white;
  box-shadow: var(--shadow-md, 0 4px 6px rgba(0, 0, 0, 0.1));
  transform: translateY(-2px);
}

/* Icône */
.trust-icon {
  flex-shrink: 0;
  width: 60px;
  height: 60px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, var(--color-accent, #ef4a21), #ff6b4a);
  border-radius: 50%;
  color: white;
  transition: transform 0.3s ease;
}

.trust-badge:hover .trust-icon {
  transform: scale(1.1) rotate(5deg);
}

/* Contenu */
.trust-content {
  flex: 1;
}

.trust-title {
  font-family: var(--font-heading, 'Playfair Display', serif);
  font-size: var(--font-size-md, 1.125rem);
  font-weight: var(--font-weight-semibold, 600);
  color: var(--color-primary, #1a1a1a);
  margin-bottom: 0.25rem;
  line-height: 1.3;
}

.trust-description {
  font-size: var(--font-size-sm, 0.875rem);
  color: var(--color-text-secondary, #666666);
  line-height: var(--line-height-normal, 1.5);
  margin: 0;
}

/* === RESPONSIVE === */
@media (max-width: 1024px) {
  .trust-badges-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }
}

@media (max-width: 768px) {
  .trust-badges-section {
    padding: 2rem 0;
  }

  .trust-badges-grid {
    grid-template-columns: 1fr;
    gap: 1rem;
  }

  .trust-badge {
    padding: 1.25rem;
    gap: 1rem;
  }

  .trust-icon {
    width: 50px;
    height: 50px;
  }

  .trust-icon svg {
    width: 28px;
    height: 28px;
  }

  .trust-title {
    font-size: var(--font-size-base, 1rem);
  }

  .trust-description {
    font-size: 0.8125rem;
  }
}
</style>
