<svg width="400" height="600" xmlns="http://www.w3.org/2000/svg">
  <!-- Fond bleu royal -->
  <rect width="400" height="600" fill="#1a237e"/>

  <!-- Bordure ornementale -->
  <rect x="25" y="25" width="350" height="550" fill="none" stroke="#ffd700" stroke-width="3" stroke-dasharray="5,5" opacity="0.5"/>
  <rect x="20" y="20" width="360" height="560" fill="none" stroke="#ffd700" stroke-width="2"/>

  <!-- Titre -->
  <text x="200" y="100" font-family="Georgia, serif" font-size="34" font-weight="bold" fill="#ffd700" text-anchor="middle">
    Likoutey
  </text>
  <text x="200" y="145" font-family="Georgia, serif" font-size="34" font-weight="bold" fill="#ffd700" text-anchor="middle">
    Tefilot
  </text>

  <!-- Sous-titre -->
  <text x="200" y="185" font-family="Georgia, serif" font-size="16" fill="#ffd700" text-anchor="middle" opacity="0.7">
    Recueil de Prières
  </text>

  <!-- Symbole mains en prière -->
  <text x="200" y="340" font-family="Arial, sans-serif" font-size="80" fill="#ffd700" text-anchor="middle" opacity="0.5">
    🙏
  </text>

  <!-- Cercles décoratifs -->
  <circle cx="200" cy="300" r="110" fill="none" stroke="#ffd700" stroke-width="1" opacity="0.3"/>
  <circle cx="200" cy="300" r="95" fill="none" stroke="#ffd700" stroke-width="1" opacity="0.3"/>

  <!-- Auteur -->
  <text x="200" y="470" font-family="Georgia, serif" font-size="18" fill="#ffd700" text-anchor="middle" opacity="0.8">
    Rabbi Nathan de Breslev
  </text>

  <!-- Éditions -->
  <text x="200" y="535" font-family="Arial, sans-serif" font-size="14" fill="#ffd700" text-anchor="middle" opacity="0.6">
    Éditions Breslev
  </text>
</svg>
