{% comment %}
  ============================================
  HERO SECTION V2 - BRESLEV BOOKS
  ============================================

  Hero section professionnelle SANS SVG amateur:
  - Vraie image de fond (ou dégradé)
  - Texte superposé avec overlay
  - 2 CTA clairs
  - Responsive parfait
  - Animations subtiles

  FINI LES SVG AVEC EMOJIS! Structure pro.

  Usage: Section dans le theme editor
{% endcomment %}

<section class="hero-v2" id="hero-breslev">

  {%comment%} Image de fond (optionnelle) {%endcomment%}
  {% if section.settings.background_image %}
    <div class="hero-background" style="background-image: url('{{ section.settings.background_image | img_url: '2000x' }}');"></div>
  {% endif %}

  {%comment%} Overlay dégradé pour lisibilité {%endcomment%}
  <div class="hero-overlay"></div>

  {%comment%} Contenu principal {%endcomment%}
  <div class="hero-content">
    <div class="hero-container">

      {%comment%} Badge/Tag (optionnel) {%endcomment%}
      {% if section.settings.show_badge %}
        <div class="hero-badge fade-in">
          {{ section.settings.badge_text | default: "Nouveauté" }}
        </div>
      {% endif %}

      {%comment%} Titre principal {%endcomment%}
      <h1 class="hero-title fade-in-up">
        {{ section.settings.title | default: "Découvrez la Sagesse de Rabbi Nachman" }}
      </h1>

      {%comment%} Sous-titre {%endcomment%}
      <p class="hero-subtitle fade-in-up" style="animation-delay: 0.2s;">
        {{ section.settings.subtitle | default: "Les enseignements authentiques de Breslev enfin accessibles" }}
      </p>

      {%comment%} Call-to-actions {%endcomment%}
      <div class="hero-cta fade-in-up" style="animation-delay: 0.4s;">
        <a href="{{ section.settings.cta_primary_link | default: '/collections/all' }}" class="btn btn-primary btn-lg">
          {{ section.settings.cta_primary_text | default: "Explorer la collection" }}
        </a>
        <a href="{{ section.settings.cta_secondary_link | default: '/pages/abonnement' }}" class="btn btn-outline btn-lg btn-white">
          {{ section.settings.cta_secondary_text | default: "Abonnement 29€/mois" }}
        </a>
      </div>

      {%comment%} Indicateurs de confiance (optionnels) {%endcomment%}
      {% if section.settings.show_trust_indicators %}
        <div class="hero-trust fade-in-up" style="animation-delay: 0.6s;">
          <div class="trust-item">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M3 10L8 15L17 5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
            <span>Livraison gratuite dès 59€</span>
          </div>
          <div class="trust-item">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M3 10L8 15L17 5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
            <span>Paiement 100% sécurisé</span>
          </div>
          <div class="trust-item">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M3 10L8 15L17 5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
            <span>Plus de 500 clients satisfaits</span>
          </div>
        </div>
      {% endif %}

    </div>
  </div>

  {%comment%} Indicateur de scroll (optionnel) {%endcomment%}
  {% if section.settings.show_scroll_indicator %}
    <div class="scroll-indicator">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" class="bounce">
        <path d="M12 5V19M12 19L5 12M12 19L19 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </div>
  {% endif %}

</section>

<style>
/* ============================================
   HERO V2 STYLES - PROFESSIONNEL
   ============================================ */

.hero-v2 {
  position: relative;
  min-height: 90vh;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  background-color: var(--color-primary, #1a1a1a);
}

/* === BACKGROUND === */
.hero-background {
  position: absolute;
  inset: 0;
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  z-index: 1;
}

/* Overlay pour lisibilité du texte */
.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    135deg,
    rgba(26, 26, 26, 0.85) 0%,
    rgba(26, 26, 26, 0.7) 50%,
    rgba(239, 74, 33, 0.3) 100%
  );
  z-index: 2;
}

/* Si pas d'image, afficher un dégradé élégant */
.hero-v2:not(:has(.hero-background)) .hero-overlay {
  background: linear-gradient(
    135deg,
    #1a1a1a 0%,
    #2a2a2a 50%,
    #1a1a1a 100%
  );
}

/* === CONTENU === */
.hero-content {
  position: relative;
  z-index: 3;
  width: 100%;
  padding: 0 2rem;
}

.hero-container {
  max-width: 900px;
  margin: 0 auto;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2rem;
}

/* Badge */
.hero-badge {
  display: inline-block;
  padding: 0.5rem 1.5rem;
  background: linear-gradient(135deg, var(--color-accent, #ef4a21), #ff6b4a);
  color: white;
  font-size: 0.875rem;
  font-weight: var(--font-weight-semibold, 600);
  text-transform: uppercase;
  letter-spacing: 1px;
  border-radius: 2rem;
  box-shadow: 0 4px 12px rgba(239, 74, 33, 0.4);
}

/* Titre principal */
.hero-title {
  font-family: var(--font-heading, 'Playfair Display', serif);
  font-size: clamp(2.5rem, 6vw, 4.5rem);
  font-weight: var(--font-weight-bold, 700);
  line-height: var(--line-height-tight, 1.2);
  color: white;
  margin: 0;
  text-shadow: 0 2px 20px rgba(0, 0, 0, 0.5);
}

/* Sous-titre */
.hero-subtitle {
  font-size: clamp(1.125rem, 2.5vw, 1.5rem);
  font-weight: var(--font-weight-normal, 400);
  line-height: var(--line-height-relaxed, 1.75);
  color: rgba(255, 255, 255, 0.9);
  max-width: 700px;
  margin: 0;
}

/* Call-to-actions */
.hero-cta {
  display: flex;
  gap: 1.5rem;
  flex-wrap: wrap;
  justify-content: center;
  margin-top: 1rem;
}

/* Bouton blanc pour fond sombre */
.btn-white {
  background-color: transparent;
  border-color: white;
  color: white;
}

.btn-white:hover {
  background-color: white;
  color: var(--color-primary, #1a1a1a);
}

/* Indicateurs de confiance */
.hero-trust {
  display: flex;
  gap: 2rem;
  flex-wrap: wrap;
  justify-content: center;
  margin-top: 1rem;
}

.trust-item {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.9375rem;
  color: rgba(255, 255, 255, 0.9);
}

.trust-item svg {
  flex-shrink: 0;
  color: var(--color-accent, #ef4a21);
}

/* === SCROLL INDICATOR === */
.scroll-indicator {
  position: absolute;
  bottom: 3rem;
  left: 50%;
  transform: translateX(-50%);
  z-index: 3;
  color: rgba(255, 255, 255, 0.8);
}

.scroll-indicator .bounce {
  animation: bounce 2s ease-in-out infinite;
}

@keyframes bounce {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(10px);
  }
}

/* === ANIMATIONS === */
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.fade-in {
  animation: fadeIn 0.8s ease-out;
}

.fade-in-up {
  animation: fadeInUp 0.8s ease-out;
}

/* === RESPONSIVE === */
@media (max-width: 1024px) {
  .hero-v2 {
    min-height: 80vh;
  }

  .hero-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
  }

  .hero-subtitle {
    font-size: clamp(1rem, 2vw, 1.25rem);
  }
}

@media (max-width: 768px) {
  .hero-v2 {
    min-height: 70vh;
  }

  .hero-content {
    padding: 0 1.5rem;
  }

  .hero-container {
    gap: 1.5rem;
  }

  .hero-cta {
    flex-direction: column;
    width: 100%;
    max-width: 400px;
    gap: 1rem;
  }

  .hero-cta .btn {
    width: 100%;
  }

  .hero-trust {
    flex-direction: column;
    gap: 1rem;
  }

  .scroll-indicator {
    bottom: 2rem;
  }
}

@media (max-width: 480px) {
  .hero-v2 {
    min-height: 60vh;
  }

  .hero-content {
    padding: 0 1rem;
  }

  .hero-title {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
  }

  .hero-subtitle {
    font-size: 1rem;
  }

  .hero-badge {
    font-size: 0.75rem;
    padding: 0.375rem 1rem;
  }

  .trust-item {
    font-size: 0.875rem;
  }
}
</style>

{% schema %}
{
  "name": "Hero Breslev V2",
  "class": "section-hero",
  "settings": [
    {
      "type": "header",
      "content": "Contenu"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Titre principal",
      "default": "Découvrez la Sagesse de Rabbi Nachman"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Sous-titre",
      "default": "Les enseignements authentiques de Breslev enfin accessibles"
    },
    {
      "type": "header",
      "content": "Boutons d'action"
    },
    {
      "type": "text",
      "id": "cta_primary_text",
      "label": "Texte bouton principal",
      "default": "Explorer la collection"
    },
    {
      "type": "url",
      "id": "cta_primary_link",
      "label": "Lien bouton principal",
      "default": "/collections/all"
    },
    {
      "type": "text",
      "id": "cta_secondary_text",
      "label": "Texte bouton secondaire",
      "default": "Abonnement 29€/mois"
    },
    {
      "type": "url",
      "id": "cta_secondary_link",
      "label": "Lien bouton secondaire",
      "default": "/pages/abonnement"
    },
    {
      "type": "header",
      "content": "Badge"
    },
    {
      "type": "checkbox",
      "id": "show_badge",
      "label": "Afficher le badge",
      "default": true
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "Texte du badge",
      "default": "Nouveauté"
    },
    {
      "type": "header",
      "content": "Visuel"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Image de fond (optionnelle)",
      "info": "Recommandé: 2000x1200px minimum. Si vide, un dégradé sera affiché."
    },
    {
      "type": "header",
      "content": "Options"
    },
    {
      "type": "checkbox",
      "id": "show_trust_indicators",
      "label": "Afficher les indicateurs de confiance",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_scroll_indicator",
      "label": "Afficher l'indicateur de scroll",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Hero Breslev V2"
    }
  ]
}
{% endschema %}
