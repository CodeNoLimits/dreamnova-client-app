{% comment %}
  ============================================
  PAGE D'ACCUEIL - BRESLEV BOOKS V2
  ============================================

  Template principal de la page d'accueil
  Structure professionnelle inspirée des benchmarks

  SECTIONS:
  1. Header (snippet)
  2. Hero Section
  3. Catégories
  4. Produits vedettes
  5. Bannière abonnement
  6. Nouveautés
  7. Rassurance
  8. Footer (snippet)
{% endcomment %}

<!DOCTYPE html>
<html lang="fr" dir="ltr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">

  <title>{{ page_title }}</title>

  {% if page_description %}
    <meta name="description" content="{{ page_description | escape }}">
  {% endif %}

  {%comment%} Preconnect pour performances {%endcomment%}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

  {%comment%} Google Fonts {%endcomment%}
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  {%comment%} Design System V2 {%endcomment%}
  {{ 'breslev-design-system-v2.css' | asset_url | stylesheet_tag }}

  {%comment%} Shopify theme CSS {%endcomment%}
  {{ content_for_header }}

  {%comment%} SEO Schema {%endcomment%}
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Bookstore",
    "name": "{{ shop.name }}",
    "description": "Librairie spécialisée dans les enseignements de Rabbi Nachman de Breslev",
    "url": "{{ shop.url }}",
    "image": "{{ shop.brand.logo | img_url: 'master' }}",
    "priceRange": "€€"
  }
  </script>
</head>

<body class="template-index">

  {%comment%} ========================================
       HEADER PRINCIPAL
       ======================================== {%endcomment%}
  {% render 'header-main' %}

  <main role="main" id="main-content">

    {%comment%} ========================================
         1. HERO SECTION
         ======================================== {%endcomment%}
    {% section 'hero-breslev-v2' %}

    {%comment%} ========================================
         2. CATÉGORIES
         ======================================== {%endcomment%}
    {% render 'categories-grid' %}

    {%comment%} ========================================
         3. PRODUITS VEDETTES
         ======================================== {%endcomment%}
    <section class="featured-products-section section">
      <div class="container">

        <div class="section-header text-center">
          <h2 class="section-title">Nos Livres Phares</h2>
          <p class="section-subtitle">Les enseignements essentiels de Rabbi Nachman</p>
        </div>

        <div class="product-grid">
          {% comment %}
            On affiche les 8 premiers produits de la collection "featured"
            ou tous les produits si la collection n'existe pas
          {% endcomment %}

          {% assign featured_collection = collections['featured'] | default: collections.all %}

          {% for product in featured_collection.products limit: 8 %}
            {% render 'book-card-v2', product: product %}
          {% endfor %}
        </div>

        <div class="section-cta text-center" style="margin-top: 3rem;">
          <a href="/collections/all" class="btn btn-primary btn-lg">
            Voir tous les livres
          </a>
        </div>

      </div>
    </section>

    {%comment%} ========================================
         4. BANNIÈRE ABONNEMENT
         ======================================== {%endcomment%}
    <section class="subscription-banner">
      <div class="container">
        <div class="banner-content">

          <div class="banner-text">
            <span class="banner-tag">Offre exclusive</span>
            <h2 class="banner-title">Abonnement Illimité Breslev Books</h2>
            <p class="banner-description">
              Accédez à toute la bibliothèque numérique pour seulement <strong>29€/mois</strong>
            </p>
            <ul class="banner-features">
              <li>
                <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                  <path d="M3 10L8 15L17 5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                </svg>
                <span>Accès illimité à tous les livres numériques</span>
              </li>
              <li>
                <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                  <path d="M3 10L8 15L17 5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                </svg>
                <span>Nouvelles sorties incluses automatiquement</span>
              </li>
              <li>
                <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                  <path d="M3 10L8 15L17 5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                </svg>
                <span>Lecture sur tous vos appareils</span>
              </li>
            </ul>
            <div class="banner-cta">
              <a href="/pages/abonnement" class="btn btn-primary btn-lg">
                S'abonner maintenant
              </a>
              <a href="/pages/abonnement" class="btn btn-outline btn-lg">
                En savoir plus
              </a>
            </div>
          </div>

          <div class="banner-visual">
            <div class="devices-mockup">
              <svg viewBox="0 0 400 300" fill="none">
                <rect x="50" y="20" width="300" height="200" rx="8" fill="#1a1a1a"/>
                <rect x="70" y="40" width="260" height="160" fill="#f5f5f5"/>
                <text x="200" y="120" text-anchor="middle" font-size="16" fill="#333">
                  📖 Bibliothèque
                </text>
              </svg>
            </div>
          </div>

        </div>
      </div>
    </section>

    {%comment%} ========================================
         5. NOUVEAUTÉS
         ======================================== {%endcomment%}
    <section class="new-products-section section bg-gray-50">
      <div class="container">

        <div class="section-header text-center">
          <h2 class="section-title">Nouveautés</h2>
          <p class="section-subtitle">Dernières parutions</p>
        </div>

        <div class="product-grid">
          {% assign new_collection = collections['nouveautes'] | default: collections.all %}

          {% for product in new_collection.products limit: 4 %}
            {% render 'book-card-v2', product: product %}
          {% endfor %}
        </div>

      </div>
    </section>

    {%comment%} ========================================
         6. RASSURANCE
         ======================================== {%endcomment%}
    {% render 'trust-badges' %}

  </main>

  {%comment%} ========================================
       FOOTER PRINCIPAL
       ======================================== {%endcomment%}
  {% render 'footer-main' %}

</body>
</html>

<style>
/* ============================================
   PAGE D'ACCUEIL - STYLES COMPLÉMENTAIRES
   ============================================ */

/* Section générique */
.section {
  padding: 4rem 0;
}

.section-header {
  margin-bottom: 3rem;
}

.section-title {
  font-family: var(--font-heading, 'Playfair Display', serif);
  font-size: var(--font-size-3xl, 2.25rem);
  font-weight: var(--font-weight-bold, 700);
  color: var(--color-primary, #1a1a1a);
  margin-bottom: 0.75rem;
}

.section-subtitle {
  font-size: var(--font-size-md, 1.125rem);
  color: var(--color-text-secondary, #666666);
  max-width: 600px;
  margin: 0 auto;
}

/* === BANNIÈRE ABONNEMENT === */
.subscription-banner {
  padding: 5rem 0;
  background: linear-gradient(135deg, #1a1a1a 0%, #2a2a2a 100%);
  color: white;
}

.banner-content {
  display: grid;
  grid-template-columns: 1.2fr 1fr;
  gap: 4rem;
  align-items: center;
}

.banner-tag {
  display: inline-block;
  padding: 0.5rem 1rem;
  background-color: var(--color-accent, #ef4a21);
  color: white;
  font-size: var(--font-size-sm, 0.875rem);
  font-weight: var(--font-weight-semibold, 600);
  text-transform: uppercase;
  letter-spacing: 1px;
  border-radius: var(--radius-full, 9999px);
  margin-bottom: 1.5rem;
}

.banner-title {
  font-family: var(--font-heading, 'Playfair Display', serif);
  font-size: clamp(2rem, 4vw, 3rem);
  font-weight: var(--font-weight-bold, 700);
  line-height: var(--line-height-tight, 1.2);
  margin-bottom: 1.25rem;
}

.banner-description {
  font-size: var(--font-size-lg, 1.25rem);
  line-height: var(--line-height-relaxed, 1.75);
  margin-bottom: 2rem;
  opacity: 0.9;
}

.banner-description strong {
  color: var(--color-accent, #ef4a21);
  font-size: 1.5em;
}

.banner-features {
  list-style: none;
  margin: 0 0 2rem;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.banner-features li {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  font-size: var(--font-size-base, 1rem);
}

.banner-features svg {
  flex-shrink: 0;
  color: var(--color-accent, #ef4a21);
}

.banner-cta {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.banner-visual {
  display: flex;
  align-items: center;
  justify-content: center;
}

.devices-mockup {
  width: 100%;
  max-width: 400px;
}

/* === RESPONSIVE === */
@media (max-width: 1024px) {
  .banner-content {
    grid-template-columns: 1fr;
    gap: 3rem;
  }

  .banner-visual {
    order: -1;
  }

  .devices-mockup {
    max-width: 300px;
  }
}

@media (max-width: 768px) {
  .section {
    padding: 3rem 0;
  }

  .section-header {
    margin-bottom: 2rem;
  }

  .section-title {
    font-size: var(--font-size-2xl, 2rem);
  }

  .subscription-banner {
    padding: 3rem 0;
  }

  .banner-title {
    font-size: clamp(1.75rem, 3vw, 2.5rem);
  }

  .banner-description {
    font-size: var(--font-size-base, 1rem);
  }

  .banner-cta {
    flex-direction: column;
  }

  .banner-cta .btn {
    width: 100%;
  }
}

@media (max-width: 480px) {
  .banner-features {
    font-size: var(--font-size-sm, 0.875rem);
  }
}
</style>
